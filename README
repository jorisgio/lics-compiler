
Il faut placer la lib ograph et licsLib dans le répertoire lib/ (ignore par git)

Pour compiler le compilateur, utiliser make.

Pour savoir comment utiliser le compilateur en détails : ./main.native
-help

En particulier, pour compiler le processeur, utiliser le script
./processor.sh
La sortie est processor.bin
